this is a contact manager API that allows users to manage a
contact list.

the app stores contact in mysql db, provides http endpoints for CRUD operations, run docker container via docker compose and save with docker volume.


the run command is docker compose up -d


